{
  "name": "git-match",
  "version": "1.0.0",
  "description": "Full Stack React application where projects of other people are displayed to the user and then the user can swipe and “match” with other projects that are interesting.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run start\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "seed": "cd server && npm run seed"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LaurenDarrimon/git-match.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/LaurenDarrimon/git-match/issues"
  },
  "homepage": "https://github.com/LaurenDarrimon/git-match#readme",
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "bootstrap-react": "^0.5.0-alpha.14"
  }
}
